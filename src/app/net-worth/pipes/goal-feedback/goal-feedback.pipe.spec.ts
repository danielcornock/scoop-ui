import { GoalFeedbackPipe } from './goal-feedback.pipe';

describe('GoalFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
